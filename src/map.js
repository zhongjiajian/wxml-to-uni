// type element attributes


// type text content


//事件
// bind bind:     @
// catch catch:   @.stop
// capture-bind:  @.capture
// capture-catch: @.capture.stop

// 属性
// model:     .sync
// wx:if      v-if
// wx:elif    v-else-if
// wx:else    v-else
// wx:for     v-for
// wx:key     :key
// wx:for-item 
// wx:for-index  (item,index) in 

// value格式
// "{{dataInfo.status == 2?'mulBtns':''}}"
// "orderDetail hasFixedFooter {{dataInfo.status == 2?'mulBtns':''}}"
// "{{type}}   {{disabled?'disabled':''}}"

// 策略：除了双括号表达式外，其余字符用\'包起来，并用+相接
